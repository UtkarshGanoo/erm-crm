# Server
PORT=5000
CORS_ORIGIN=http://localhost:5173

# JWT
JWT_SECRET=change_this_to_a_long_random_string
JWT_EXPIRES_IN=8h

# Database - EITHER use DATABASE_URL (Neon/Render/Railway) OR the individual DB_* vars below
# DATABASE_URL=postgresql://user:password@host:5432/dbname
# DB_SSL=true

DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=erp_crm
